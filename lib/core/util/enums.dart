enum RequestState {
  loading,
  success,
  failure,
}

enum ToastStates {
  success,
  error,
  warning,
}

enum BottomState {
  isInitial,
  loading,
  success,
  failure,
}

enum ToggleStates {
  isStarted,
  isSelected,
}

enum UploadImageStates {
  isInitial,
  success,
  failure,
}

enum TaskStates {
  inProgress,
  waiting,
  finished,
}

enum PriorityStates {
  low,
  medium,
  high,
}
